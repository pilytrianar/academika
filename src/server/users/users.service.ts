/*import { prisma } from '@/server';

// Return all users (only emails and IDs)
export async function getAllUsers() {
  const users = await prisma.user.findMany({ select: { id: true, email: true } });
  return users;
}*/
